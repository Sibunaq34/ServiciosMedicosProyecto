@page "{idUsuario:int}"
@model ServiciosMedicos.Pages.Usuarios.EditarModel

<h2>Editar Usuario</h2>

<form method="post">

    <input asp-for="Usuario.IdUsuario" type="hidden" />

    <div class="mb-3">
        <label class="form-label">Nombre de Usuario</label>
        <input asp-for="Usuario.UsuarioNombre" class="form-control" />
    </div>

    <div class="mb-3">
        <label class="form-label">Nombre Completo</label>
        <input asp-for="Usuario.NombreCompleto" class="form-control" />
    </div>

    <div class="mb-3">
        <label class="form-label">Correo</label>
        <input asp-for="Usuario.Correo" class="form-control" />
    </div>

    <div class="mb-3">
        <label class="form-label">Rol</label>
        <select asp-for="Usuario.IdRol" class="form-control">
            @foreach (var rol in Model.ListaRoles)
            {
                <option value="@rol.IdRol">
                    @rol.NombrePermiso
                </option>
            }
        </select>
    </div>

    <button type="submit" class="btn btn-primary">
        Actualizar
    </button>

    <a asp-page="Index" class="btn btn-secondary">
        Volver
    </a>

</form>