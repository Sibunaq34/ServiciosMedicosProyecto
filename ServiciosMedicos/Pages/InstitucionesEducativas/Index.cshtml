@page
@model ServiciosMedicos.Pages.InstitucionesEducativas.IndexModel
@{
    ViewData["Title"] = "Instituciones Educativas";
}

<div class="container-fluid px-4 py-4">
    <h2 class="fw-bold mb-1">Instituciones Educativas</h2>
    <hr class="mb-3">

    <a class="btn btn-primary mb-3" asp-page="./Crear">Nuevo</a>

    @await Html.PartialAsync(
        "_MensajeModal",
        Tuple.Create(Model.MensajeExito, Model.MensajeError))

    <div class="table-responsive">
        <table class="table table-bordered table-hover align-middle">
            <thead style="background-color:#1a2e42;">
                <tr>
                    <th class="text-white" scope="col">Código</th>
                    <th class="text-white" scope="col">Nombre</th>
                    <th class="text-white text-end" scope="col">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Instituciones.Count == 0)
                {
                    <tr>
                        <td colspan="3" class="text-center text-muted">
                            No hay instituciones educativas registradas.
                        </td>
                    </tr>
                }
                else
                {
                    @foreach (var institucion in Model.Instituciones)
                    {
                        var modalId = $"eliminarInstitucion{institucion.IdInstitucion}";

                        <tr>
                            <td>@institucion.Codigo</td>
                            <td>@institucion.Nombre</td>
                            <td>
                                <div class="d-flex flex-column flex-sm-row gap-1 justify-content-end">
                                <a class="btn btn-sm me-1" style="background-color:#5BBFFF; color:#12375C;"
                                   asp-page="./Editar"
                                   asp-route-id="@institucion.IdInstitucion">
                                    Editar
                                </a>
                                <button type="button"
                                        class="btn btn-sm"
                                        style="background-color:#5BBFFF; color:#12375C;"
                                        data-bs-toggle="modal"
                                        data-bs-target="#@modalId">
                                    Eliminar
                                </button>
                                </div>

                                <div class="modal fade"
                                     id="@modalId"
                                     tabindex="-1"
                                     aria-labelledby="@($"{modalId}Titulo")"
                                     aria-hidden="true">
                                    <div class="modal-dialog">
                                        <div class="modal-content text-start">
                                            <div class="modal-header">
                                                <h2 class="modal-title h5" id="@($"{modalId}Titulo")">
                                                    Confirmar eliminación
                                                </h2>
                                                <button type="button"
                                                        class="btn-close"
                                                        data-bs-dismiss="modal"
                                                        aria-label="Cerrar">
                                                </button>
                                            </div>
                                            <div class="modal-body">
                                                ¿Realmente desea eliminar el elemento seleccionado?
                                            </div>
                                            <div class="modal-footer">
                                                <button type="button"
                                                        class="btn btn-secondary"
                                                        data-bs-dismiss="modal">
                                                    No
                                                </button>
                                                <form method="post"
                                                      asp-page-handler="Eliminar"
                                                      asp-route-idInstitucion="@institucion.IdInstitucion"
                                                      asp-route-pagina="@Model.Pagina">
                                                    <button type="submit" class="btn btn-danger">
                                                        Sí
                                                    </button>
                                                </form>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>

    <nav aria-label="Paginación de instituciones educativas">
        <ul class="pagination flex-wrap">
            <li class="page-item @(Model.Pagina <= 1 ? "disabled" : "")">
                <a class="page-link"
                   asp-page="./Index"
                   asp-route-pagina="@(Model.Pagina - 1)">
                    Anterior
                </a>
            </li>
            <li class="page-item active" aria-current="page">
                <span class="page-link">@Model.Pagina</span>
            </li>
            <li class="page-item @(Model.HaySiguientePagina ? "" : "disabled")">
                <a class="page-link"
                   asp-page="./Index"
                   asp-route-pagina="@(Model.Pagina + 1)">
                    Siguiente
                </a>
            </li>
        </ul>
    </nav>
</div>
